# Customer Support

Skills for the Customer Support department — Customer 360 lookups and brand-voice rules.

Clone into your tool of choice (Cursor, Codex, Windsurf, Claude Code's `~/.claude/skills/`). Wire up the MCP servers in `.mcp.json` in your tool before invoking the Skills.
